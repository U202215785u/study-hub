# Study Hub 0.6 Release Closure Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use subagent-driven-development or executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Produce a verified 0.6.0 release candidate from the current `master` source without publishing or deploying it.

**Architecture:** Keep application version metadata aligned across the Vue frontend, Python backend, and Electron desktop shell. Capture the release scope and known operating constraints in a changelog, then validate source behavior using the backend suite, frontend suite/build, and browser persistence check. Git delivery-gate work remains a separately audited taskboard component until its branch passes its own test and review gate.

**Tech Stack:** Vue 3/Vite/Vitest/Playwright, FastAPI/Pytest, Electron, Git, Dashi Taskboard.

---

### Task 1: Repair Release-Gate Regressions

**Files:**
- Modify: `study-hub/frontend/src/composables/home/useDashboardFlip.js`
- Test: `study-hub/frontend/src/composables/home/useDashboardFlip.test.js`
- Modify: `study-hub/backend/tests/test_document_pagination.py`

- [x] **Step 1: Add a Flip callback-contract assertion and observe it fail against `enter` / `leave`.**

- [x] **Step 2: Use the GSAP Flip callback names `onEnter` and `onLeave`; rerun the focused Vitest test.**

- [x] **Step 3: Reproduce the full backend ordering failure (141 collected, 1 failed) and constrain the pagination test to its own `pagination-contract-` search scope with `finally` cleanup.**

- [x] **Step 4: Verify `test_document_pagination.py` and `home-layout-persistence.mjs` pass.**

### Task 2: Align 0.6.0 Version Metadata

**Files:**
- Modify: `study-hub/frontend/package.json`
- Modify: `study-hub/backend/pyproject.toml`
- Modify: `study-hub/electron/package.json`

- [x] **Step 1: Change each application package version to `0.6.0`. Do not alter independently versioned browser extension or workflow schemas.**

- [x] **Step 2: Validate parsability and exact values.**

Run:

```powershell
node -e "for (const file of ['frontend/package.json','electron/package.json']) { console.log(file, require('./' + file).version) }"
py -3 -c "import tomllib; print(tomllib.load(open('backend/pyproject.toml', 'rb'))['project']['version'])"
```

Expected: all three values are `0.6.0`.

### Task 3: Write Release Candidate Notes

**Files:**
- Create: `study-hub/CHANGELOG.md`

- [x] **Step 1: Add a `0.6.0` section dated 2026-08-09 that records the verified dashboard, task-category, knowledge-base, parser, search, and release-gate work.**

- [x] **Step 2: Record deliberate scope limits: no deployment, taskboard Git-delivery gate remains separately audited until integrated, six unresolved knowledge documents remain unmodified, and visual summaries require the source image URL at analysis time.**

### Task 4: Run Candidate Validation

**Files:**
- Verify only; no source changes expected.

- [x] **Step 1: Run the complete backend suite.**

```powershell
py -3 -m pytest backend/tests -q
```

Expected: all 141 tests pass; existing deprecation warnings are recorded but do not mask failures.

- [x] **Step 2: Run frontend tests, production build, and browser layout persistence.**

```powershell
npm run test:unit
npm run build
$env:STUDY_UI_ORIGIN='http://127.0.0.1:5173'; node tests/home-layout-persistence.mjs
```

Expected: all tests pass, build exits 0, and the persisted drag position survives reload.

- [x] **Step 3: Run `git diff --check` and inspect the final candidate diff.**

### Task 5: Resolve Taskboard Delivery-Gate Inclusion

**Files:**
- Verify: `dashi-taskboard` repository and `codex/taskboard-git-delivery-gate` branch.

- [x] **Step 1: Finish the independent branch audit, including Node 24 typecheck, build, tests, and implementation/design comparison.**

- [x] **Step 2: Only merge the branch after its review finds no release-blocking gaps and the dashi-taskboard worktree is clean. Otherwise document it as excluded from the 0.6 candidate.**

### Task 6: Record Candidate and Request Release Approval

**Files:**
- Update: taskboard issue comments/statuses for verified in-review items.
- Record: Butler evidence for the parent release case.

- [x] **Step 1: Move only user-approved, fully verified `in_review` issues to `done`; leave backlog and excluded work open with an explicit release-scope note.**

- [x] **Step 2: Record source SHA, validation evidence, included/excluded scope, and release risks.**

- [ ] **Step 3: Request explicit user approval before any release, deployment, or production-state operation.**
