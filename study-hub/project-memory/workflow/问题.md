# Workflow 模块问题清单

## 问题格式
- ISS-WF-XXX: [问题描述] — [严重级别: critical/high/medium/low] — [发现日期] — [状态: open/tracking/resolved]

## 待决问题

- ISS-WF-003: `before`/`after` gate 失败后的处理 —— high —— 2026-06-06 —— open
  - after gate 不满意 → 回退到上一步重做？还是只重做当前步？
  - 需在 YAML 中支持 `on_fail: retry_current | retry_previous | stop`

- ISS-WF-005: workflow 嵌套 —— low —— 2026-06-06 —— open
  - 一个 workflow 的某个步骤本身是另一个 workflow

- ISS-WF-007: `human` 类型步骤无超时机制 —— low —— 2026-06-06 —— open

- ISS-WF-008: `after` gate 的 `retry` 无限循环风险 —— medium —— 2026-06-06 —— open

## 已解决问题

- ISS-WF-001: 暂停恢复 —— 已实现 `_do_pause()` + `resume()` + 前端轮询
- ISS-WF-002: 模板变量 —— 已实现 `_VAR_RE` + `_resolve_var()` 路径解析
- ISS-WF-004: 并发步骤 —— 框架预留 parallel 扩展点
- ISS-WF-006: `_pause_reason` 未暴露 —— v1.1 已修复（engine.status() 增加字段）
- ISS-WF-009: 引擎参数传反导致任务永远排队 —— critical —— 2026-06-06 —— resolved
  - `_executor.submit(_run, task_id, template)` 但 `_run(template, task_id)` 参数顺序反了
  - 步骤一执行就抛异常被静默吞掉，任务状态永远 pending
  - 修复：统一为 `_executor.submit(_run, template, task_id, ...)`
- ISS-WF-010: AI 调用 SSL 失败 —— critical —— 2026-06-06 —— resolved
  - httpx 在 ThreadPoolExecutor 线程中 SSL 握手不稳定（UNEXPECTED_EOF）
  - 修复：改用 `requests` 库 + `verify=False` 降级 + 3 次重试（DEC-WF-012）
