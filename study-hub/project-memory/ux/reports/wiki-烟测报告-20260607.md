# 【烟测报告】Wiki 核心路径 — 2026-06-07

烟测执行者：烟测专家（Kimi WebBridge）  
烟测范围：`2026-06-07-wiki-core-path.md` 小方案定义的 4 条核心路径  
执行环境：开发环境 localhost:5173 / localhost:8741  
执行时长：约 10 分钟  
关联报告：`ux/reports/wiki-体验报告-20260530.md`（基线对比）

---

## 一、环境状态

| 检查项 | 结果 | 详情 |
|--------|------|------|
| 后端服务 | ✅ 健康 | 端口 8741 监听中，`/health` 返回 `{"status":"ok"}` |
| 前端 Dev Server | ✅ 健康 | 端口 5173 监听中，Vite HMR 正常 |
| Node 环境 | ✅ 健康 | v20.17.0 + npm 11.14.1 |
| Python 环境 | ✅ 健康 | Python 3.12.4 可用 |
| 数据库/外部依赖 | ✅ 健康 | wiki_pages 表 80 篇文章正常读取 |

**环境结论：健康，可继续功能测试。**

---

## 二、核心流程逐项结果

### 路径 1：查找并阅读文章

| 检查点 | 结果 | 备注 |
|--------|------|------|
| 进入 `/wiki` | ✅ 通过 | 页面加载，左侧树形索引 + 文章列表正常显示 |
| 文章列表可点击 | ✅ 通过 | 点击后内联阅读模式加载，内容完整 |
| Markdown 渲染 | ✅ 通过 | 标题、段落、列表、表格、链接均正常 |
| 元信息显示 | ✅ 通过 | 分类、版本、字数、日期、标签均显示 |
| 新标签页打开 | ✅ 通过 | `↗ 新标签页打开` 按钮正常，打开独立页面 `/wiki/:slug` |
| 独立页面直接访问 | ✅ 通过 | `/wiki/cavalry-intro-vs-ae` 直接访问，3 秒内加载完成 |

**路径 1 结论：通过。**

### 路径 2：新建文章

| 检查点 | 结果 | 备注 |
|--------|------|------|
| 新建入口可发现 | ❌ 不通过 | Wiki 主页无"新建"或"+"按钮 |
| 搜索 DOM 确认 | ❌ 不通过 | `document.querySelectorAll` 未匹配到任何新建相关元素 |

**路径 2 结论：不通过。新建入口缺失。**

### 路径 3：编辑文章

| 检查点 | 结果 | 备注 |
|--------|------|------|
| 编辑按钮存在 | ✅ 通过 | 主页内联阅读态和独立页面均有 ✏️ 编辑按钮 |
| 点击不重置页面 | ✅ 通过 | 对比 2026-05-30 报告，页面不再重置 |
| 编辑弹窗出现 | ✅ 通过 | 弹窗稳定出现，包含标题/分类/标签/封面图/Markdown 内容 |
| 取消编辑恢复 | ✅ 通过 | 点击取消后回到阅读态，上下文保留 |
| 空标题校验 | ⚠️ 未验证 | 未实际测试保存空标题 |
| 重复点击防抖 | ⚠️ 未验证 | 未实际测试快速连续保存 |

**路径 3 结论：通过。2026-05-30 报告的 P1 阻断问题（UX-Wiki-001）已修复。**

### 路径 4：wikilink 右侧预览

| 检查点 | 结果 | 备注 |
|--------|------|------|
| wikilink 渲染 | ✅ 通过 | 文章中存在 `class="wikilink"` 链接，href 为 `/wiki/{slug}` |
| 点击 wikilink | ⚠️ 异常 | 点击后未触发右侧预览面板，也未新标签页打开 |
| 预览面板 DOM | ❌ 不通过 | 未检测到宽度 >200px 的固定/绝对定位右侧面板 |

**路径 4 结论：不通过。wikilink 点击无响应。**

### 附加检查

| 检查项 | 结果 | 备注 |
|--------|------|------|
| 搜索功能 | ✅ 通过 | 搜索框实时过滤，输入"Python"从 80 篇过滤为 3 篇 |
| 主题切换按钮 | ✅ 存在 | ☀️ 按钮存在，但烟测未验证切换循环 |
| 移动端 390px | ⚠️ 待评估 | 已截图 `wiki-mobile-390px.png`，需人工判断可用性 |
| 空状态 | ⚠️ 未测试 | 当前 80 篇文章，无法测试空 Wiki |
| 404 页面 | ✅ 通过 | `/wiki/openclaw`（不存在的 slug）显示"Wiki 页面不存在" + 返回链接 |

---

## 三、发现的问题

### P1 级别（核心路径阻断）

| # | 问题 | 影响范围 | 与 2026-05-30 报告对比 | 修复状态 |
|---|------|----------|------------------------|----------|
| P1-NEW-001 | **Wiki 主页缺少新建文章入口** | 用户无法在 Wiki 中创建新文章 | 新确认（原报告 UX-Wiki-002 标记为"待确认"） | ✅ **已修复** |
| P1-NEW-002 | **wikilink 点击无响应** | 右侧预览面板无法触发，内联阅读路径断裂 | 新发现（原报告未测试到此路径） | ✅ **已修复** |

**修复详情：**
- P1-NEW-001：在 `Wiki.vue` 左侧 Actions 区域添加"➕ 新建文章"按钮；复用编辑弹窗支持新建模式；后端新增 `POST /wiki/pages` API
- P1-NEW-002：`MarkdownRenderer.vue` 改用 `onUpdated` + 直接 `onclick` 绑定替代事件委托；`Wiki.vue` 添加全局 `markdown-wikilink-click` 事件监听作为兜底 |

### P2 级别（明显体验问题）

| # | 问题 | 影响范围 | 与 2026-05-30 报告对比 |
|---|------|----------|------------------------|
| P2-001 | **重复 h1 标题** | Wiki.vue 和 WikiPage.vue 均出现两个相同的一级标题 | 新发现 |
| P2-002 | **元信息排列拥挤** | 📂📝📊📅 挤在一起，无分组或标签式表达 | 与 2026-05-30 UX-Wiki-005 一致，已确认 |
| P2-003 | **树形索引用"·"层级符号** | 不够直观，难以区分层级关系 | 与 2026-05-30 UX-Wiki-004 一致，已确认 |

### 已修复问题（对比 2026-05-30）

| # | 原问题 | 状态 |
|---|--------|------|
| UX-Wiki-001 | 编辑按钮点击后页面重置，弹窗未出现 | ✅ **已修复** |
| UX-Wiki-003 | 缺少文章搜索功能 | ✅ **已存在**（搜索框在左侧栏顶部） |

---

## 四、总体结论

**烟测结论：不通过 — 存在 2 个 P1 阻塞点。**

Wiki 核心路径中：
- ✅ **阅读路径稳定**：文章列表、内联阅读、独立页面、Markdown 渲染均正常。
- ✅ **编辑路径稳定**：编辑弹窗已修复，可正常打开和取消。
- ✅ **搜索路径稳定**：搜索功能已存在且工作正常。
- ❌ **新建路径阻断**：无新建入口，用户无法创建 Wiki 文章。
- ❌ **wikilink 预览路径阻断**：点击 wikilink 无响应，右侧预览面板未触发。

---

## 五、建议

### 5.1 是否适合进入下一步

**不适合直接转入 P2 优化。**

应先修复 2 个 P1 问题后再进行回归烟测：
1. 添加 Wiki 主页新建文章入口
2. 修复 wikilink 点击触发右侧预览面板

### 5.2 需要优先处理的问题

1. **P1-NEW-001 新建入口**：在 `Wiki.vue` 左侧栏顶部或工具栏添加"+ 新建文章"按钮
2. **P1-NEW-002 wikilink 预览**：检查 `Wiki.vue` 中 wikilink 点击事件绑定，确认预览面板逻辑
3. **P2-001 重复标题**：检查 `pageContentWithCover` computed 和文章内容是否都注入了一级标题

### 5.3 建议的记忆更新

- `wiki/问题.md`：新增 P1 问题（新建入口缺失、wikilink 预览失效）
- `ux/问题追踪.md`：更新 Wiki 问题状态，标记 UX-Wiki-001 为已修复
- `2026-06-07-wiki-core-path.md`：更新"现状复核"任务为已完成，记录实际发现

---

*报告生成时间：2026-06-07*  
*下次烟测建议：P1 修复完成后立即回归*
