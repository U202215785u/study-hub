# 全局决策日志

## 决策记录格式
- DEC-XXX: [决定] — [原因] — [日期] — [状态: active/superseded]

## 活跃决策
- DEC-001: brainstorm 选项格式固定为 `- [文本]` — 解析器硬依赖 — 2026-05-29 — active
- DEC-002: brainstorm 两种模式共用 step2/step3 端点，通过 mode 字段区分 — 减少端点数量 — 2026-05-29 — active
- DEC-003: 学习计划用 Markdown 文件而非数据库存储 — 便于用户直接编辑 — 2026-05-29 — active
- DEC-004: checklist 完成状态暂存前端 localStorage — 简化实现，后续如需多端同步再考虑后端 — 2026-05-29 — active
- DEC-005: ASR 采用三级降级策略（URL → ffmpeg → 失败）— 最大化成功率 — 2026-05-29 — **superseded**
  - 替代决策：DEC-005a（抖音单层策略）
  - 原因：抖音 URL 直传 DashScope 几乎注定失败（防盗链），Level 1 浪费 API 调用；降级策略增加代码复杂度和错误诊断难度
- DEC-005a: 抖音 ASR 采用单层策略（下载 → ffmpeg 提取音频 → ASR）— 简化实现、提升错误可诊断性 — 2026-05-30 — active
- DEC-006: 深度总结使用 Claude Code 而非直接 API — 质量优先 — 2026-05-29 — active
- DEC-007: 任务队列 max_workers=3 — 防止并发过多导致限流 — 2026-05-29 — active
- DEC-008: 支持批量提交（换行分隔或数组）— 提升多链接处理效率 — 2026-05-29 — active
- DEC-009: Wiki 编译输出 JSON 格式 — 结构化便于后续处理 — 2026-05-29 — active
- DEC-010: 采用增量编译策略（content_hash 缓存）— 减少重复 LLM 调用 — 2026-05-29 — active
- DEC-011: wikilink 使用 [[slug]] 格式 — 简洁且便于正则提取 — 2026-05-29 — active
- DEC-012: 向量模型使用 BAAI/bge-small-zh-v1.5 — 中文效果优于英文模型 — 2026-05-29 — active
- DEC-013: 前端使用 Tailwind CSS 工具类为主 — 开发效率高 — 2026-05-29 — active
- DEC-014: 圆角统一 12px（大容器）/ 8px（按钮输入框）— 视觉一致性 — 2026-05-29 — active
- DEC-015: API 封装集中在 settings.js Pinia store — 统一入口便于维护 — 2026-05-29 — active
- DEC-016: API 返回统一格式 `{status, ...}` 或 `{error, detail}` — 前后端一致 — 2026-05-29 — active
- DEC-017: 数据库使用 SQLite + WAL — 简化部署，单文件可移植 — 2026-05-29 — active
- DEC-018: AI 客户端锁定 DeepSeek — 统一模型行为可预期 — 2026-05-29 — active
- DEC-019: Embedding 优先本地模型，失败回退到 API — 降低成本和延迟 — 2026-05-29 — active
- DEC-020: esbuild 使用 WASM 版本绕过 360 拦截 — 360 会替换原生 esbuild 二进制并破坏 IPC 通信，esbuild-wasm 纯 WebAssembly 不受影响 — 2026-05-29 — active
- DEC-021: Windows 本地后台启动使用 `cmd /c start /min` + PowerShell PID 追踪 — PowerShell `Start-Process` 子进程会被父会话 Job Object 连带销毁，`cmd /c start /min` 可创建真正独立的进程 — 2026-05-29 — active
- DEC-022: Windows 前端构建必须使用无空格工作目录 — 项目路径 `study web` 含空格导致 Vite ESM 模块解析崩溃（exit code 13），通过 `build.sh` 复制到 `/c/temp/study-hub-fe-build` 构建后产物回拷 — 2026-05-29 — active
- DEC-023: 多代理并发操作防护三件套 — 解决反复 shell / 构建产物覆盖 / 端口冲突 / 数据库重置问题 — 2026-05-30 — active
  1. 后台启动脚本：`start-background.ps1` + `stop-background.ps1` + `.bat` 封装，使用 `cmd /c start /min` 创建独立进程，PowerShell 追踪 PID，启动时校验端口占用和僵尸 PID 清理
  2. 构建互斥锁：`build.sh` 增加 `/c/temp/study-hub-fe-build.lock` 文件锁，5分钟超时，防止多代理同时构建导致 `dist/` 产物随机覆盖
  3. 数据库防重置：`database.py` `init_db()` 增加 `db_exists` 判断，已有数据文件时跳过 `CREATE TABLE` 初始化，避免多实例竞争导致表重建
  4. Service Worker 清理：部署清理型 `sw.js` 注销旧项目缓存，解决旧 PWA 残留导致的页面劫持
  5. apiBase 策略变更：从"同源部署"改为"开发代理 + 预览直连"，当前硬编码 `http://localhost:8741`，长期应改为环境变量配置（见 ISS-021）
- DEC-024: Markdown 样式采用 Kami 纸面模式（局部暖色 + 暗色外壳共存）— 用户选择 BAA 方案：仅 Markdown 内容区暖色、引入仓耳今楷字体、全页面生效；保持应用外壳暗色主题不变，降低改动风险 — 2026-05-30 — active
- DEC-025: Markdown 区域支持三档主题切换（米白/牛皮纸/暗黑）— 用户追加需求：每个 Markdown 区域右上角浮动按钮循环切换、localStorage 持久化、默认跟随系统 prefers-color-scheme；牛皮纸底色为用户指定 `#66462a`，强调色暖金 `#e8d5b5`；暗黑主题恢复原始暗色配色 — 2026-05-30 — active
- DEC-026: AI 团队协作系统三项优化 — 解决 @skill 歧义 + 管家自动触发 + 记忆索引化 — 2026-05-30 — active
  1. @ 歧义：CLAUDE.md 定义正确加载语法（自然语言触发，非 @文件路径）；8 个 SKILL.md 顶部新增自执行声明
  2. 管家自动触发：CLAUDE.md 定义进入对话后自动读取状态.md + 索引.md，主动输出路由建议
  3. 记忆索引化：新建 project-memory/索引.md，汇总全局记忆、模块状态、skill 速查、技术栈、高频路径
- DEC-027: 禁止手动 `python main.py` 启动后端，统一使用 `后台启动.bat` — 避免野进程、统一进程管理、确保 PID 文件写入 — 2026-05-30 — active
  - 背景：ISS-027 发现手动启动的服务无 PID 文件，导致 stop 脚本找不到进程、端口冲突、代码修改后不生效
  - 规定：
    1. 所有 Agent 启动后端必须使用 `后台启动.bat` → `start-background.ps1`
    2. 所有 Agent 停止后端必须使用 `后台停止.bat` → `stop-background.ps1`
    3. 禁止直接使用 `python backend/main.py`、`uvicorn main:app`、`py main.py` 等任何手动启动方式
    4. 如果脚本因环境问题失败，先修脚本/环境，而不是绕过脚本手动启动
  - 相关：ISS-025（python 命令失效）、ISS-026（venv 污染）、ISS-027（野进程）
- DEC-028: Markdown 排版系统改用 Kami 原版 margin 模型 — 解决 Flow 间距系统与中文内容不匹配的问题 — 2026-05-30 — active
  - 背景：Flow 系统（`> * + *`）把所有元素一视同仁，导致标题前后、段落前后、引用块前后间距无法精确控制，中文长段落下显得拥挤
  - 变更：
    1. 抛弃 Flow 变量系统（`--flow-space` / `--flow-space-loose` / `--flow-space-tight` / `--flow-space-medium`）
    2. 每个元素照搬 Kami 原版 margin（p `mb: 10pt`、h2 `margin: 24pt 0 8pt`、h3 `margin: 18pt 0 6pt`、ul `margin: 6pt 0 10pt`、blockquote `margin: 12pt 0` 等）
    3. line-height 1.55 → 1.75（中文舒适阅读）
    4. 列表项之间去掉额外 margin，纯靠 line-height 撑开（Kami 原版）
    5. 列表 marker 改为藏青色（Kami 原版）
  - 相关：DOM 结构修复（MarkdownRenderer.vue 按钮外移，避免破坏 `.markdown-content` 直接子元素关系）
- DEC-029: 五层记忆系统 v2 架构设计 — 解决"跨 AI 工具失忆"核心痛点 — 2026-05-30 — **superseded**
  - 替代：无（功能废弃）
  - 原因：五层记忆系统整体废弃，相关代码和文件已全部删除 — 2026-06-06
  - 背景：用户在不同 AI 工具（Kimi/Claude/ChatGPT）间切换时完全失忆，每次都要重复项目背景、个人偏好、技术选型
  - 五层模型：
    1. 角色记忆（Role）— "我是谁"：个人偏好、习惯、能力边界
    2. 项目记忆（Project）— "我在做什么"：项目进度、决策、技术栈
    3. 工作流记忆（Workflow）— "我怎么做的"：开发流程、工具偏好
    4. 会话记忆（Session）— "刚才聊了什么"：单次对话上下文
    5. 世界记忆（World）— "外部知识"：知识库、文档、收藏
  - 收集策略：会话记忆自动收集 → AI 提炼 → 分层存储（角色/工作流需用户确认）
  - 注入策略：对话开始时自动注入用户画像 + 活跃项目 + 相关工作流
  - 跨工具共享：浏览器插件（Kimi 等网页）+ MCP Server（Claude Desktop）+ Skill（Kimi Code CLI）统一访问同一 SQLite 后端
- DEC-030: 浏览器插件请求必须通过 background.js Service Worker 代理 — 绕过 CORS 限制 — 2026-05-30 — active
  - 背景：网页（如 kimi.com）直接 fetch localhost:8741 会被浏览器 CORS 策略拦截
  - 方案：content script 通过 `chrome.runtime.sendMessage` 发送请求 → background.js Service Worker 通过 `fetch` 访问后端 → 结果返回 content script
  - 附加：CORS 中间件 allow_origins 改为 `"*"`，允许所有来源
- DEC-031: 浏览器插件支持用户自定义 AI 网站 — 不限于内置 5 个网站 — 2026-05-30 — active
  - 背景：用户可能使用 Gemini、文心一言、通义千问等其他 AI 工具
  - 方案：
    1. manifest.json host_permissions 改为 `https://*/*`，匹配所有 https 网站
    2. 未配置的网站显示 `⚙️ 配置此网站` 按钮
    3. 点击后弹窗自动检测对话元素候选，用户点击选择
    4. 配置保存到 chrome.storage.sync，持久化存储
    5. 刷新页面后自动加载自定义适配器
- DEC-032: 浏览器插件采用可视化元素选择器 + 选中文字采集双模式 — 解决固定选择器脆弱问题 — 2026-05-30 — active
  - 背景：固定 CSS 选择器方案（DEC-031）在 Kimi 新域名上失效，且网页改版后容易断裂
  - 方案：
    1. 保留自动采集模式，但选择器由用户通过可视化界面自定义
    2. 新增"选中文字采集"模式（📝 记住这段），不依赖页面结构
    3. 可视化配置：鼠标悬停高亮元素 → 点击保存 → 生成 CSS 选择器 → 存储到 chrome.storage.sync
    4. 选择器生成策略：优先使用有意义的 class → 回退到父元素路径
    5. 适用场景：选中采集用于任意网页的任意内容；可视化配置用于固定结构的对话页面
  - 相关：ISS-035（选择器匹配错误内容）
- DEC-034: Wiki AI 封面图服务选择 GrsAi Nano Banana 2 — 性价比最优 — 2026-05-30 — active
  - 背景：用户需要为 Wiki 文章生成 AI 封面图，先后评估了 image2api（需自建+ChatGPT Plus）、百炼通义万相（¥0.2/张，效果一般）、GrsAi Nano Banana 2（¥0.065/张）
  - 方案对比：
    1. image2api：需自建服务+ChatGPT Plus 订阅，有封号风险，部署复杂
    2. 百炼通义万相：国内直连，但价格较高（¥0.2/张），效果偏卡通
    3. GrsAi Nano Banana 2：国内直连，价格最低（¥0.065/张），16:9 宽屏适合博客封面，失败返还积分
  - 决策：选择 GrsAi Nano Banana 2 作为默认生图服务
  - 接口：非 OpenAI 兼容，需异步提交任务+轮询结果，后端已封装为同步式 `POST /images/generate-cover`
  - 文件：`backend/endpoints/images.py`
- DEC-033: 五层记忆系统 v2.1 最小修复集 — 解决"记忆存了但用不上"核心体验问题 — 2026-05-30 — **superseded**
  - 替代：无（功能废弃）
  - 原因：五层记忆系统整体废弃，相关代码和文件已全部删除 — 2026-06-06
- DEC-035: 搜索助手浮窗技术方案 — 浏览器扩展注入 Bing 页面提供场景化搜索辅助 — 2026-05-31 — active
  - 背景：用户搜索时面临广告淹没、低质量内容、找不到官网等痛点，传统 AI 总结方案太慢（5-20s）且质量不稳定
  - 方案演进：
    1. v1（放弃）：AI 总结搜索结果 → 太慢，用户不愿等
    2. v2（放弃）：跳 Bing + 后台总结 → 用户操作太快，总结跟不上
    3. v3（当前）：扩展注入 Bing 页面，旁侧浮窗提供场景化辅助
  - 核心设计：
    1. 不干预主搜索流程 — 用户正常看 Bing 结果，浮窗在右侧辅助
    2. 场景化推荐 — 根据搜索词自动匹配场景（工具查找/技术文档/找官网/产品测评/教程学习）
    3. 来源筛选 — 用户可勾选只查看特定网站，自动改写 URL 加 site: 过滤
    4. 速度优先 — 场景规则本地静态配置，毫秒级响应
  - 技术实现：
    1. manifest.json 新增 Bing 页面 content_script 匹配
    2. bing-assistant.js — 匹配场景 → 注入 Shadow DOM 浮窗
    3. assistant-panel.css — 完全自包含样式，通过 Shadow DOM 隔离 Bing CSS
    4. scene-rules.json — 场景定义、关键词映射、来源配置
  - 关键决策：
    1. 浮窗位置：右侧固定面板，有 Knowledge Panel 时替换 KP 位置
    2. 筛选执行：页面内 URL 改写 + 原地重载（不开新标签页）
    3. 来源记忆：隐式记忆 + 透明 UI（自动勾选上次选择，灰色圆点标记）
    4. 触发时机：Bing 搜索页全自动注入（不再限制 ref=studyhub）+ 一键关闭恢复（右下角小圆点）
  - 已知问题：
    1. 扩展需手动重新加载才能生效（content script 修改后）
    2. 中文编码需用 Unicode 转义（\uXXXX）避免 Shadow DOM 内乱码
    3. createPanel() 需检测 host.shadowRoot 有效性（已修复）
  - 待实现：AI 分析区域（流式 SSE）、场景规则后端动态更新
- DEC-036: 搜索助手浮窗 v1.0 体验优化 — 解决产品经理走查发现的 5 个设计问题 — 2026-05-31 — active
  - 背景：用户搜索"gpt"时浮窗显示"找官网"场景，推荐为空、来源单薄、AI 区域占位、提示重复
  - 问题与修复：
    1. 场景匹配错误（ISS-040）：sessionStorage 恢复旧场景覆盖新搜索 → 新搜索始终重新匹配，筛选刷新才恢复原场景
    2. 找官网内容单薄（ISS-041）：来源只有一个"官网" → 新增 GitHub、Product Hunt；推荐为空 → 添加官网查询技巧引导
    3. AI 区域占位（ISS-042）：大面积占位文案 → 改为可折叠，点击标题展开/收起
    4. 空推荐生硬（ISS-043）："暂无推荐" → 引导性空状态（🔍 图标 + 标题 + 操作建议）
    5. 关键词覆盖不足：技术文档场景新增 gpt/llm/ai/深度学习/神经网络/机器学习等关键词
  - 关键决策：
    1. 去掉 ref=studyhub 限制 — 浮窗在任意 Bing 搜索页显示，用户可直接在 Bing 使用
    2. AI 分析默认展开但可折叠 — 不隐藏功能，但给用户控制权
    3. 所有场景补充推荐内容 — 避免任何场景出现空推荐
  - 背景：体验测试报告（2026-05-30）发现五层模型分类精美但注入链路存在结构性死锁，且注入内容是"信息描述"而非"行为指令"
  - 方案（三件事）：
    1. 文件注入器：后端定时生成 CLAUDE.md（Claude Code）+ .cursor/rules/memory-context.mdc（Cursor）+ .claude/skills/memory-context/SKILL.md（Kimi Code CLI），覆盖三个主流 AI 工具
    2. 世界记忆检索：context_inject 新增 documents + wiki_pages 查询，取 top 2，知识库不再沉睡
    3. 角色记忆优化：混合加权排序（access_count×0.5 + confidence×0.3 + recency×0.2）取 top 3，token 减少 70%；注入格式改为行为指令式（"因为用户有这个偏好，所以你应该..."）
  - 技术实现：
    - backend/core/memory_ranker.py：混合加权排序模块
    - backend/core/file_injector.py：文件生成逻辑（三种格式）
    - backend/main.py：asyncio 定时任务（启动后30秒首次执行，之后每5分钟）
    - backend/endpoints/memory.py：context_inject 改行为指令式 + 世界记忆检索 + 路由顺序修复
  - 关键决策：
    - Cursor 格式用 .cursor/rules/*.mdc（新格式，alwaysApply）而非 legacy .cursorrules
    - Kimi skill 需用户手动加载（Kimi Code CLI 不自动发现项目级 skill）
    - 定时任务用 asyncio.create_task 而非 APScheduler（零依赖）
  - 相关：体验测试报告 2026-05-30
