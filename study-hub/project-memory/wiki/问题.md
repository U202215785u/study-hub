# Wiki 模块问题清单

## 问题格式
- ISS-XXX: [问题描述] — [严重级别: critical/high/medium/low] — [发现日期] — [状态: open/tracking/resolved]

## 开放问题
- ISS-007: compiled_hashes.json 与 wiki_pages 表不同步 — medium — 2026-05-29 — open
  - 影响：手动删除 wiki 页面后无法重新编译
  - 建议：删除 wiki 页面时同步清理 compiled_hashes
- ISS-016: Wiki 页面当前以弹窗/内嵌框展示，应改为独立页面 + 新标签页跳转 — medium — frontend/wiki — 2026-05-29 — resolved
  - 影响：当前 Wiki 页面点击后在当前页内以弹窗/小框展示，空间受限、阅读体验差、无法利用浏览器前进后退
  - 建议：
    1. Wiki 页面改为独立路由（如 `/wiki/:slug`），占满整个视口
    2. 点击页面列表中的条目时，在新标签页打开（`target="_blank"` 或 `window.open`）
    3. 保留当前 Wiki 知识库主页作为导航入口
  - 解决：
    - 新增 `WikiPage.vue` 独立页面组件，占满整个视口
    - 新增路由 `/wiki/:slug`
    - Wiki 列表、搜索列表点击 → `<a target="_blank">` 新标签页
    - 知识图谱节点点击 → `window.open` 新标签页
    - wikilink 渲染改为 `<a target="_blank">`
    - Wiki 主页保留为导航入口，移除内联展示死代码
    - 主页 Wiki 入口改为 `<a target="_blank">` 新标签页打开
    - 左侧分类改为树形索引 + 下方文章列表，解决分类导航和文章入口问题
    - 右侧预览面板：wikilink 点击弹出预览，支持深入/切换/关闭
    - `openPage` 通过 API 获取完整内容，修复列表点击后右侧内容空白问题
    - App.vue 中 Wiki 路由隐藏 NavBar/SystemStatus，Wiki.vue 全屏占满视口

## 已解决问题
- ISS-023: 设置封面图后显示"Wiki 页面不存在" — high — wiki/frontend — 2026-05-30 — **resolved**
  - 根因：`update_wiki_page` 中无条件重新 `slugify(title)`，若原始 slug 因标题重复带有 `-id` 后缀，重新 slugify 会丢失后缀。前端 `saveEdit()` 仍用旧 slug 调用 `loadPage()`，导致 404
  - 修复：
    1. 后端 `backend/endpoints/wiki.py`（第 456-462 行）：仅在 title 变化时才重新生成 slug，否则保持原 slug
    2. 前端 `frontend/src/views/WikiPage.vue`（第 307-311 行）：保存成功后若后端返回的 slug 与当前 URL 不同，执行 `router.replace` 更新 URL
  - 验证：title 不变时（如只改封面图），slug 保持原值，不会丢失 `-id` 后缀；title 变化导致 slug 变化时，前端自动更新 URL，不会 404
