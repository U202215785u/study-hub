# Frontend 模块问题清单

## 问题格式
- ISS-XXX: [问题描述] — [严重级别: critical/high/medium/low] — [发现日期] — [状态: open/tracking/resolved]

## 开放问题
- ISS-004: Toast 组件在 Brainstorm.vue 和 Home.vue 中重复实现 — low — 2026-05-29 — open
  - 影响：代码冗余，样式不一致
  - 建议：抽取为独立 Toast 组件
- ISS-005: accent 色 `#7c8aff` 及变体硬编码在多个 Vue 文件 — low — 2026-05-29 — open
  - 影响：换主题时需要改多处
  - 建议：定义 CSS 变量或在 Tailwind config 中扩展
- ISS-014: 知识库文档预览弹窗尺寸过小、内容溢出、样式不美观 — medium — 2026-05-29 — **resolved**
  - 修复：弹窗改为全屏抽屉样式（w-[98%] h-[95vh]），内容区 flex-1 自适应，增加 bg-bg 背景和 padding
  - 文件：`frontend/src/views/Home.vue`

## 开放问题
- ISS-017: 知识库最近文档列表缺少复制和删除操作按钮 — low — frontend — 2026-05-29 — open
  - 影响：用户无法快速复制文档内容或删除不需要的文档
  - 待确认（实现者第一步必须完成）：
    1. 定位文档列表组件：读取前端代码，找到「最近文档」列表的渲染位置（可能在 `Home.vue` / `KnowledgeBase.vue` / 其他文件）
    2. 确认条目 DOM 结构：当前条目右侧是否已有占位元素？还是完全空白？
    3. 确认复制内容：复制文档标题？全文 Markdown？还是文档链接？
    4. 确认删除接口：调用哪个后端 API 删除文档？是否需要二次确认弹窗？
  - 建议：先读代码输出以上 4 点确认，再执行按钮添加

## 已解决问题
- ISS-015: 自动化解析缺少进度可视化与解析队列展示 — medium — frontend/automation — 2026-05-29 — **resolved**
  - 修复：后端细粒度步骤更新 + 前端卡片进度条 + 全局队列抽屉面板 + 3 秒轮询
  - 文件：`frontend/src/views/Home.vue`、`frontend/src/components/TaskStatusBadge.vue`、`backend/endpoints/automation.py`
- ISS-011: Vue 前端 Vite 构建在 Windows 下崩溃（esbuild 被 360 拦截）— critical — 2026-05-29 — **resolved**
  - 根因：`node_modules/@esbuild/win32-x64/esbuild.exe` 被 360 替换为 `RuntimeBroker` 代理程序，破坏 IPC 通信
  - 修复：`frontend/package.json` 添加 `overrides: { esbuild: "npm:esbuild-wasm@0.21.5" }`，使用 WASM 版本绕过拦截
  - 文件：`frontend/package.json`
- ISS-013: 项目路径含空格导致 Vite 构建失败（exit code 13）— critical — 2026-05-29 — **resolved**
  - 根因：路径 `study web` 含空格，Windows Git Bash 下 Vite ESM 模块解析崩溃，`npm run build` 无产物生成
  - 修复：构建前将 `frontend/` 复制到无空格临时目录（如 `/c/temp/study-hub-fe/`），构建完成后将 `dist/` 产物复制回原项目
  - 文件：`frontend/dist/`（构建产物目录）
