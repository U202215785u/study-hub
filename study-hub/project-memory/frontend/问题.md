# Frontend 模块问题清单

## 问题格式
- ISS-XXX: [问题描述] — [严重级别: critical/high/medium/low] — [发现日期] — [状态: open/tracking/resolved]

## 开放问题
- ISS-004: Toast 组件在 Brainstorm.vue 和 Home.vue 中重复实现 — low — 2026-05-29 — open
  - 影响：代码冗余，样式不一致
  - 建议：抽取为独立 Toast 组件
- ISS-005: accent 色 `#7c8aff` 及变体硬编码在多个 Vue 文件 — low — 2026-05-29 — open
  - 影响：换主题时需要改多处
  - 建议：定义 CSS 变量或在 Tailwind config 中扩展
- ISS-014: 知识库文档预览弹窗尺寸过小、内容溢出、样式不美观 — medium — 2026-05-29 — **resolved**
  - 修复：弹窗改为居中近全屏（w-[92%] md:w-[88%] max-w-[1200px] max-h-[90vh]），内容区 flex-1 自适应，增加 bg-bg 背景和 padding
  - 文件：`frontend/src/views/Home.vue`、`frontend/src/views/KnowledgeBase.vue`
  - 2026-05-30 修正：Home.vue 原始尺寸为 w-[98%] h-[95vh]，与 KnowledgeBase.vue 不一致，现已统一为相同尺寸

## 开放问题
- ISS-021: apiBase 硬编码 `http://localhost:8741`，部署到非本机环境会失效 — high — frontend — 2026-05-30 — open
  - 影响：当前修复将 `apiBase` 从 `''` 改为 `'http://localhost:8741'`，解决了生产预览模式无数据的问题，但引入了新隐患——项目移到另一台机器、Docker 容器或服务器部署时，API 请求会指向不存在的 `localhost:8741`
  - 根因：`settings.js` 第 6 行 `import.meta.env.DEV ? '/api' : 'http://localhost:8741'` 是硬编码判断，没有配置化入口
  - 建议：改为从环境变量读取，如 `import.meta.env.VITE_API_BASE || (import.meta.env.DEV ? '/api' : '')`，在 `.env` 文件配置 `VITE_API_BASE=http://localhost:8741`
  - 文件：`frontend/src/stores/settings.js`

- ISS-017: 知识库最近文档列表缺少复制和删除操作按钮 — low — frontend — 2026-05-29 — open
  - 影响：用户无法快速复制文档内容或删除不需要的文档
  - 待确认（实现者第一步必须完成）：
    1. 定位文档列表组件：读取前端代码，找到「最近文档」列表的渲染位置（可能在 `Home.vue` / `KnowledgeBase.vue` / 其他文件）
    2. 确认条目 DOM 结构：当前条目右侧是否已有占位元素？还是完全空白？
    3. 确认复制内容：复制文档标题？全文 Markdown？还是文档链接？
    4. 确认删除接口：调用哪个后端 API 删除文档？是否需要二次确认弹窗？
  - 建议：先读代码输出以上 4 点确认，再执行按钮添加

- ISS-023: 设置封面图后显示"Wiki 页面不存在" — high — wiki/frontend — 2026-05-30 — **resolved**
  - 根因：`update_wiki_page` 中无条件重新 `slugify(title)`，若原始 slug 因标题重复带有 `-id` 后缀，重新 slugify 会丢失后缀。前端 `saveEdit()` 仍用旧 slug 调用 `loadPage()`，导致 404
  - 修复：
    1. 后端 `backend/endpoints/wiki.py`（第 456-462 行）：仅在 title 变化时才重新生成 slug，否则保持原 slug
    2. 前端 `frontend/src/views/WikiPage.vue`（第 307-311 行）：保存成功后若后端返回的 slug 与当前 URL 不同，执行 `router.replace` 更新 URL
  - 验证：title 不变时（如只改封面图），slug 保持原值，不会丢失 `-id` 后缀；title 变化导致 slug 变化时，前端自动更新 URL，不会 404

- ISS-022: 首页知识库按钮未新标签页打开，知识库页面未撑满全屏 — medium — frontend/ui — 2026-05-30 — **resolved**
  - 修复：Home.vue「知识库管理」链接改为 `<a href="/kb" target="_blank">`；App.vue 全屏判断扩展为 `/kb` 路由；KnowledgeBase.vue 高度从 `h-[calc(100vh-64px)]` 改为 `h-screen`
  - 文件：`frontend/src/views/Home.vue`、`frontend/src/App.vue`、`frontend/src/views/KnowledgeBase.vue`

## 已解决问题
- ISS-019: 多代理同时构建导致 frontend/dist 产物随机覆盖 — high — 2026-05-30 — **resolved**
  - 根因：`build.sh` 使用固定临时目录 `/c/temp/study-hub-fe-build`，多代理并发构建无互斥，两个代理同时执行时互相覆盖源码和产物
  - 修复：`build.sh` 增加 `/c/temp/study-hub-fe-build.lock` 文件锁机制（标记文件 + 5分钟超时），第二个构建会等待第一个完成
  - 已知限制：异常退出（kill -9 / 断电）时 `trap cleanup_lock EXIT` 不会执行，锁文件可能残留最多 5 分钟。超时后自动清理。
  - 文件：`frontend/build.sh`
- ISS-015: 自动化解析缺少进度可视化与解析队列展示 — medium — frontend/automation — 2026-05-29 — **resolved**
  - 修复：后端细粒度步骤更新 + 前端卡片进度条 + 全局队列抽屉面板 + 3 秒轮询
  - 文件：`frontend/src/views/Home.vue`、`frontend/src/components/TaskStatusBadge.vue`、`backend/endpoints/automation.py`
- ISS-011: Vue 前端 Vite 构建在 Windows 下崩溃（esbuild 被 360 拦截）— critical — 2026-05-29 — **resolved**
  - 根因：`node_modules/@esbuild/win32-x64/esbuild.exe` 被 360 替换为 `RuntimeBroker` 代理程序，破坏 IPC 通信
  - 修复：`frontend/package.json` 添加 `overrides: { esbuild: "npm:esbuild-wasm@0.21.5" }`，使用 WASM 版本绕过拦截
  - 文件：`frontend/package.json`
- ISS-013: 项目路径含空格导致 Vite 构建失败（exit code 13）— critical — 2026-05-29 — **resolved**
  - 根因：路径 `study web` 含空格，Windows Git Bash 下 Vite ESM 模块解析崩溃，`npm run build` 无产物生成
  - 修复：构建前将 `frontend/` 复制到无空格临时目录（如 `/c/temp/study-hub-fe/`），构建完成后将 `dist/` 产物复制回原项目
  - 文件：`frontend/dist/`（构建产物目录）
