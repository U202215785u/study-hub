# Study-Hub 五层记忆系统 — 全流程使用指南

> 版本：v2.0 | 最后更新：2026-05-30

---

## 一、系统概述

### 什么是五层记忆系统？

一个**自动收集、分层存储、智能注入**的 AI 记忆系统。让不同 AI 工具（Kimi、Claude、ChatGPT 等）共享同一份记忆，实现"一次记录，处处可用"。

### 五层记忆模型

```
👤 角色记忆 (Role)      — "我是谁"       → 个人偏好、习惯、能力
📁 项目记忆 (Project)   — "我在做什么"   → 项目进度、决策、技术栈
⚙️ 工作流记忆 (Workflow) — "我怎么做的"   → 开发流程、工具偏好、协作模式
💬 会话记忆 (Session)   — "刚才聊了什么" → 单次对话上下文
🌍 世界记忆 (World)     — "外部知识"     → 知识库、文档、收藏
```

### 核心流程

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  对话进行    │────→│  自动提取    │────→│  分层存储    │
│  (任何AI)   │     │  (浏览器插件) │     │  (本地SQLite) │
└─────────────┘     └─────────────┘     └──────┬──────┘
                                                │
┌─────────────┐     ┌─────────────┐            │
│  新对话开始  │←────│  智能注入    │←───────────┘
│  (任何AI)   │     │  (MCP/Skill) │
└─────────────┘     └─────────────┘
```

---

## 二、安装配置

### 步骤 1：确保后端运行

```bash
# 进入项目目录
cd "F:/360MoveData/Users/Administrator/Desktop/study web/study-hub"

# 启动后端
./venv/Scripts/HD_python.exe backend/main.py

# 或 Windows 后台启动
./后台启动.bat
```

验证：`http://localhost:8741/health` 返回 `{"status":"ok"}`

### 步骤 2：安装浏览器插件

```
1. 打开 Chrome/Edge，进入扩展管理页
2. 开启"开发者模式"
3. 点击"加载已解压的扩展"
4. 选择文件夹：study-hub/extension/
5. 确认扩展已启用，显示"学习中枢 - 对话采集"
```

**支持的网站：**
- ✅ Kimi (kimi.moonshot.cn)
- ✅ Claude (claude.ai)
- ✅ ChatGPT (chat.openai.com)
- ✅ DeepSeek (chat.deepseek.com)
- ✅ 豆包 (doubao.com)

### 步骤 3：配置 Claude Desktop MCP（可选）

编辑 Claude Desktop 配置文件：

**Mac:** `~/Library/Application Support/Claude/claude_desktop_config.json`
**Windows:** `%APPDATA%/Claude/claude_desktop_config.json`

```json
{
  "mcpServers": {
    "study-hub": {
      "command": "F:/360MoveData/Users/Administrator/Desktop/study web/study-hub/venv/Scripts/HD_python.exe",
      "args": [
        "F:/360MoveData/Users/Administrator/Desktop/study web/study-hub/backend/mcp_server.py"
      ],
      "env": {
        "STUDY_HUB_API_BASE": "http://localhost:8741",
        "DASHSCOPE_API_KEY": "your-api-key"
      }
    }
  }
}
```

重启 Claude Desktop。

### 步骤 4：验证安装

```bash
# 测试 API
curl http://localhost:8741/memory/list

# 应该返回记忆列表（刚开始为空）
{"items": [], "total": 0, "limit": 50, "offset": 0}
```

---

## 三、日常使用流程

### 场景 1：自动收集记忆（最常用）

**你在 Kimi 上聊了一个项目：**

```
你：我想做一个个人博客，用 Next.js + MDX
Kimi：好的，Next.js 的 App Router 很适合...
（聊了 20 分钟，讨论了路由、样式、部署）

你：先到这里吧，谢谢
Kimi：不客气，有问题随时问
```

**系统做了什么（自动，无需你操作）：**

1. 检测到结束语"先到这里"、"谢谢"
2. 自动调用 `/memory/summarize_and_extract`
3. AI 分析对话，提取记忆：
   - 💬 **会话记忆**："讨论了 Next.js App Router 的路由方案"
   - 📁 **项目记忆**："个人博客项目，技术栈 Next.js + MDX"
   - 👤 **角色记忆**："用户偏好使用现代前端框架"（需确认）

4. 保存到本地数据库

**你在浏览器右下角看到：**
```
🧠 已提取 3 条记忆
```

---

### 场景 2：跨工具记忆共享

**第二天，你在 Claude 上继续：**

```
你：继续写那个博客
```

**Claude（通过 MCP 自动注入上下文）：**

```
Claude：好的，继续个人博客项目。上次我们讨论了 Next.js App Router 
的路由方案，技术栈是 Next.js + MDX。目前进度是...

需要我先回顾一下已完成的模块，还是直接开始新的部分？
```

**Claude 怎么知道的？**

1. 对话开始时，MCP 自动调用 `get_context_profile`
2. 查询到"个人博客"项目记忆
3. 注入到系统提示中

---

### 场景 3：手动记录偏好

**你在任何 AI 工具上说：**

```
你：记住，我喜欢用 Vim 快捷键，不用鼠标
AI：好的，记住了
```

**方式 A：浏览器插件**

1. 点击右下角的 🧠 **记住这段**
2. 系统自动提取并分类
3. 如果是角色记忆，弹出确认：
   ```
   检测到关于你的新信息：
   - "用户喜欢使用 Vim 快捷键"
   
   [记住] [忽略] [编辑]
   ```

**方式 B：Claude Desktop MCP**

```
你：记住我喜欢用 Vim
Claude（调用 remember 工具）：
✅ 已记住 (ID 42): 用户喜欢使用 Vim 快捷键
分类: preferences | 标签: vim, editor
```

**方式 C：前端页面**

1. 打开 `http://localhost:8741/memory`
2. 点击"+ 新建记忆"
3. 填写内容，选择层级"👤 角色记忆"
4. 保存

---

### 场景 4：查看和管理记忆

**打开记忆管理页面：**

```
http://localhost:8741/memory
```

**左侧导航：**

```
记忆层级
  🧠 全部记忆 (12)
  👤 角色记忆 (3)      ← 点击只看角色层
  📁 项目记忆 (4)      ← 点击只看项目层
  ⚙️ 工作流记忆 (2)    ← 点击只看工作流层
  💬 会话记忆 (8)      ← 点击只看会话层
  🌍 世界记忆 (0)

状态
  🧠 全部状态
  ✅ 有效
  🕐 已过时
  ❌ 错误

项目
  🟢 个人博客
  🟡 电商后台
```

**记忆卡片示例：**

```
✅ active  [👤 角色]  ID 42  2026-05-30
用户喜欢使用 Vim 快捷键，偏好键盘操作
#vim #editor

[编] [🕐] [❌] [删]
```

```
✅ active  [📁 项目: 个人博客]  ID 45  2026-05-30
博客路由已完成，使用文件系统路由方案
#nextjs #routing

[编] [🕐] [❌] [删]
```

---

### 场景 5：更新项目进度

**你在 Claude 上完成了博客路由：**

```
你：博客路由写完了，用的文件系统路由
Claude：太好了！需要我记住这个进度吗？
你：是的，更新记忆
```

**Claude（调用 summarize_conversation）：**

```
🧠 对话已分析，提取 2 条记忆

分层统计:
- 角色记忆: 0 条
- 项目记忆: 1 条
- 工作流记忆: 0 条
- 会话记忆: 1 条

检测到的项目: 个人博客
```

**系统自动：**
1. 更新"个人博客"项目的进度
2. 新增项目记忆："路由已完成，使用文件系统路由"

---

### 场景 6：纠正错误记忆

**AI 记错了：**

```
Claude：根据你的记忆，你喜欢用 Emacs...
你：不对，我喜欢的是 Vim，不是 Emacs
```

**方式 A：自然语言纠正**

```
你：forget "我喜欢 Emacs"，reason: wrong
Claude（调用 forget 工具）：
✅ 已自动匹配并标记记忆 #15 为 wrong。
内容: 用户喜欢使用 Emacs
```

**方式 B：前端操作**

1. 打开 `http://localhost:8741/memory`
2. 搜索"Emacs"
3. 找到记忆，点击 ❌ 标记为错误

---

### 场景 7：工作流偏好学习

**你连续几次都用同样的方式写代码：**

```
对话 1：先写测试，再写实现
对话 2：先写测试，再写实现
对话 3：先写测试，再写实现
```

**系统检测到模式：**

```
检测到重复工作流模式：
- "先写测试再实现" 出现了 3 次

是否记录为工作流偏好？
[记录为 TDD] [忽略] [编辑]
```

**你点击"记录"：**

```
⚙️ 工作流记忆已创建：
名称: coding
偏好: {tdd: true, test_first: true}
触发关键词: ["写代码", "coding", "实现"]
```

**下次你说"写代码"时，AI 自动问：**

```
Claude：好的，按你的工作流偏好，要先写测试吗？
```

---

## 四、各层记忆详解

### 👤 角色记忆 — "我是谁"

**存储内容：**
- 个人偏好：编码风格、沟通方式、技术倾向
- 能力边界：擅长什么、不擅长什么
- 工作习惯：作息、工作流偏好

**收集方式：**
- 自动提取（高置信度）
- 用户说"记住这个"
- 手动添加

**使用方式：**
- 每次对话开始时自动注入
- 格式："用户是全栈开发者，偏好 Vue + Python..."

**示例：**
```json
{
  "content": "用户喜欢先写测试再实现代码",
  "memory_layer": "role",
  "memory_type": "habit",
  "category": "coding",
  "confidence": 0.95
}
```

---

### 📁 项目记忆 — "我在做什么"

**存储内容：**
- 项目清单：名称、描述、状态
- 项目进度：完成了什么、阻塞点、下一步
- 项目决策：为什么选这个方案

**收集方式：**
- 自动检测项目名称
- 对话中提及项目时自动关联
- 手动创建项目

**使用方式：**
- 检测到项目关键词时注入
- 格式："项目[博客]状态：路由完成，待 contentlayer..."

**示例：**
```json
{
  "content": "博客路由已完成，使用文件系统路由",
  "memory_layer": "project",
  "project_name": "个人博客",
  "memory_type": "progress",
  "confidence": 1.0
}
```

---

### ⚙️ 工作流记忆 — "我怎么做的"

**存储内容：**
- 开发流程：TDD、分支策略、代码审查
- 工具偏好：IDE、快捷键、脚本
- 协作模式：如何与 AI 配合

**收集方式：**
- 观察重复行为模式
- 用户显式配置
- 手动添加

**使用方式：**
- 执行对应任务时注入
- 格式："工作流偏好：先写测试再实现..."

**示例：**
```json
{
  "content": "使用 TDD 开发，先写测试再实现",
  "memory_layer": "workflow",
  "workflow_name": "coding",
  "memory_type": "preference",
  "confidence": 0.9
}
```

---

### 💬 会话记忆 — "刚才聊了什么"

**存储内容：**
- 本次对话核心内容
- 已做出的决定
- 待办事项

**收集方式：**
- 完全自动，无需确认
- 对话结束时自动提取

**使用方式：**
- 同一会话内保持连续性
- 新会话不注入（已提炼到上层）

**示例：**
```json
{
  "content": "讨论了三种路由方案，选择文件系统路由",
  "memory_layer": "session",
  "memory_type": "discussion",
  "confidence": 1.0
}
```

---

### 🌍 世界记忆 — "外部知识"

**存储内容：**
- 知识库文档
- 收藏的代码片段
- 技术文章

**收集方式：**
- 知识库上传
- Wiki 编译
- 手动收藏

**使用方式：**
- RAG 检索，按需注入
- 不自动注入，只在相关时检索

---

## 五、命令速查

### 浏览器插件操作

| 操作 | 方式 |
|------|------|
| 自动提取开关 | 点击右下角 🤖 按钮 |
| 手动提取记忆 | 点击右下角 🧠 记住这段 |
| 采集到知识库 | 点击右下角 采集到学习中枢 |
| 查看统计 | 点击扩展图标打开 popup |

### MCP 工具（Claude Desktop）

| 工具 | 使用场景 |
|------|---------|
| `get_context_profile` | 对话开始时自动调用 |
| `summarize_conversation` | 对话结束时调用 |
| `remember` | 用户说"记住这个" |
| `recall` | 搜索相关记忆 |
| `forget` | 用户纠正记忆 |
| `list_projects` | 查看项目列表 |
| `list_workflows` | 查看工作流偏好 |

### CLI 命令

```bash
cd study-hub/backend

# 添加记忆
python memory_cli.py add "我喜欢用 Vim" --cat preferences

# 搜索记忆
python memory_cli.py recall "编辑器偏好"

# 列出记忆
python memory_cli.py list --limit 20

# 删除记忆
python memory_cli.py delete 42
```

### API 调用

```bash
# 分层提取对话
curl -X POST http://localhost:8741/memory/summarize_and_extract \
  -H "Content-Type: application/json" \
  -d '{"conversation": "对话内容...", "source_tool": "kimi"}'

# 获取上下文注入
curl "http://localhost:8741/memory/context_inject?q=继续博客项目"

# 列出项目
curl http://localhost:8741/projects

# 列出工作流
curl http://localhost:8741/workflows
```

---

## 六、常见问题

### Q1：记忆没有自动提取？

**检查：**
1. 后端是否运行？`http://localhost:8741/health`
2. 插件是否安装并启用？
3. 自动提取开关是否开启？（右下角 🤖 按钮）
4. 对话是否超过 200 字？

**解决：**
```bash
# 重启后端
cd study-hub
./后台停止.bat
./后台启动.bat
```

### Q2：跨工具记忆不共享？

**检查：**
1. Claude Desktop MCP 配置是否正确？
2. `STUDY_HUB_API_BASE` 是否指向正确地址？
3. 后端是否允许跨域？（已默认配置）

**测试：**
```bash
# 在 Kimi 聊完后，检查是否有新记忆
curl http://localhost:8741/memory/list?layer=session
```

### Q3：记忆太多导致噪音？

**解决：**
1. 定期清理会话记忆（保留 30 天）
2. 将重要记忆升级到角色/项目层
3. 标记错误/过时的记忆

### Q4：隐私顾虑？

**说明：**
- 所有数据存储在本地 SQLite
- 不上传云端
- 可以关闭自动提取，改为手动模式
- 敏感记忆可以标记为"不共享"

---

## 七、最佳实践

### 1. 养成"更新记忆"习惯

每次重要对话结束时说：
```
"更新记忆"
"同步记忆"
"记住今天的讨论"
```

### 2. 主动确认角色记忆

当系统提示检测到角色记忆时：
```
[记住] ← 确认保存到长期记忆
[忽略] ← 只是临时情况
[编辑] ← 修改后再保存
```

### 3. 定期整理记忆

每周花 5 分钟：
1. 打开 `http://localhost:8741/memory`
2. 查看"💬 会话记忆"，删除无用的
3. 将重要的会话记忆"升级"到项目/角色层

### 4. 项目命名规范

使用统一的项目名称，方便自动关联：
```
✅ "个人博客"、"电商后台"、"学习笔记"
❌ "那个博客项目"、"上次说的"、"那个东西"
```

### 5. 工作流命名规范

```
coding      — 写代码相关
debugging   — 调试相关
writing     — 写作相关
learning    — 学习相关
reviewing   — 代码审查
```

---

## 八、记忆质量优化

### 提升提取准确率

1. **对话要具体**：
   ```
   ✅ "我用 Next.js App Router 做博客路由"
   ❌ "我做那个东西"
   ```

2. **明确表达偏好**：
   ```
   ✅ "我喜欢先写测试，这是我的习惯"
   ❌ "嗯，先写测试吧"
   ```

3. **定期纠正错误**：
   ```
   "不对，我喜欢的是 Vim，不是 Emacs"
   ```

### 减少噪音

1. **及时标记过时**：
   ```
   "这个已经不用了，标记为过时"
   ```

2. **删除测试记忆**：
   ```
   "删除这条测试用的记忆"
   ```

---

## 九、进阶用法

### 自定义工作流

```bash
# 创建工作流
curl -X POST http://localhost:8741/workflows \
  -H "Content-Type: application/json" \
  -d '{
    "name": "code_review",
    "description": "代码审查工作流",
    "trigger_keywords": ["review", "审查", "代码检查"],
    "preferences": {"strict": true, "focus_on": "naming"}
  }'
```

### 批量导入记忆

```python
# 从 Markdown 文件导入
import requests

memories = [
    {"content": "...", "memory_layer": "role", "memory_type": "preference"},
    # ...
]

for mem in memories:
    requests.post("http://localhost:8741/memory/remember", json=mem)
```

### 记忆导出备份

```bash
# 导出 SQLite 数据库
cp study-hub/backend/data/study_hub.db backup/study_hub.db.$(date +%Y%m%d)
```

---

## 十、故障排除

| 问题 | 检查 | 解决 |
|------|------|------|
| 插件不显示按钮 | 扩展是否启用？ | 重新加载扩展 |
| API 返回 404 | 后端是否运行？ | 重启后端 |
| MCP 工具报错 | API_BASE 配置？ | 检查配置文件 |
| 提取质量差 | AI 模型是否正常？ | 检查 DASHSCOPE_API_KEY |
| 记忆不注入 | 记忆层是否正确？ | 检查 role 层是否有数据 |
| 跨工具不共享 | 是否同一数据库？ | 确认后端地址一致 |

---

## 附录：记忆注入示例

### Claude 看到的系统提示（自动注入）

```
[系统提示：用户记忆画像]
===
【用户画像】
- 用户是全栈开发者，偏好 Vue + Python
- 用户喜欢先写测试再实现代码（TDD）
- 用户习惯晚上写代码，是夜猫子
- 用户偏好使用 Vim 快捷键

【活跃项目】
- 个人博客: 路由已完成，待 contentlayer (技术栈: Next.js, MDX)
- 电商后台: 用户管理模块开发中 (技术栈: React, Node.js)

【工作流偏好】
- coding: tdd=true, test_first=true

【相关记忆】
- [项目] 讨论了三种路由方案，选择文件系统路由
- [会话] 上次讨论了 contentlayer 的配置问题
===
```

---

*本指南随系统更新而更新。如有问题，查看 `study-hub/project-memory/状态.md` 获取最新状态。*
